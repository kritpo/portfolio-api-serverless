resumeLangGET:
    handler: src/infra/resumes.resumeLangGET
    events:
        - http:
              path: /resumes/{username}/languages
              method: get
              cors: true
              request:
                  parameters:
                      paths:
                          username: true
