resumeLangGET:
    handler: src/infra/resumesLanguages.resumeLangGET
    events:
        - http:
              path: /resumes/{username}/languages
              method: get
              cors: true
              request:
                  parameters:
                      paths:
                          username: true
